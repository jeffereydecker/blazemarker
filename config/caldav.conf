# CalDAV Configuration for Radicale Integration
#
# SECURITY: For better security, set these as environment variables instead:
#   export CALDAV_SERVER_URL=http://localhost:5232
#   export CALDAV_USERNAME=jdecker
#   export CALDAV_PASSWORD=your_secure_password
#   export CALDAV_CALENDAR=
#
# Environment variables take precedence over this file.
# If you use env vars, you can leave this file empty or delete sensitive values.
#
# This file should be kept secure (add to .gitignore)

# Radicale server URL (default port is 5232)
CALDAV_SERVER_URL=https://dav.blazemarker.com

# Your username
CALDAV_USERNAME=jdecker

# Your Radicale password (CONSIDER USING ENV VAR INSTEAD!)
CALDAV_PASSWORD=degneb-6carsa-gisJit

# Optional: Specific calendar name (leave empty to use first calendar)
CALDAV_CALENDAR=
